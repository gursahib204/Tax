<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tax">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Tax">
        <activity android:name=".emicalc"></activity>
        <activity android:name=".incomeTax" />
        <activity android:name=".taxcalculator" />
        <activity android:name=".blogtwo" />
        <activity android:name=".blogone" />
        <activity android:name=".cardView" />
        <activity android:name=".MainActivity2" />
        <activity android:name=".signUp" />
        <activity android:name=".loginPage" />
        <activity
            android:name=".splashScreen"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>